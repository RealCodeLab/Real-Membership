<?php

if ( defined( 'REAL_MEMBERSHIP_LOADED' ) ) {
	return;
}

define( 'REAL_MEMBERSHIP_VERSION', '0.1.0' );
define( 'REAL_MEMBERSHIP_LOADED', true );

/*
 * Codestar Framework
 */
if ( ! defined( 'CS_ACTIVE_LIGHT_THEME' ) ) {
	define( 'CS_ACTIVE_LIGHT_THEME', true );
}
